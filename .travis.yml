language: python

python:
  - 3.8

install:
  - pip install pylint
  - pip install -r requirements.txt

script:
  - find . -name "*.py" -not -path "./.git/*" | xargs pylint --max-line-length=100 --disable=too-few-public-methods

